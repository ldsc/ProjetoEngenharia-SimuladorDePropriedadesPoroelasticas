@startuml
' Título do Diagrama
title Diagrama de Comunicação - Processamento de Amostra

' --- Estilo Visual e de Leiaute ---
' Força as linhas a serem retas (ortogonais)
skinparam linetype ortho
' Aumenta o espaçamento vertical (ranksep) e horizontal (nodesep)
skinparam ranksep 100
skinparam nodesep 150

skinparam object {
    BackgroundColor #FEFBDC
    BorderColor #D3B45F
    ArrowColor #C82D22
    FontColor #000000
}
skinparam note {
    BackgroundColor #FEFBDC
    BorderColor #D3B45F
}

' --- Definição dos Objetos ---
object ":CSimulator" as Sim
object ":CInput" as Input
object ":CElasticCalculator" as Calc
object ":CReservoirData" as ResData
object ":CPlotManager" as Plot
object ":CMineralDatabase" as DB


' --- Relacionamentos e Mensagens (organizados de cima para baixo) ---
Sim -down-> Input   : "1: ReadMultipleFromFile()"
Sim -down-> Calc    : "2: Calculate(sample)"
Sim -down-> ResData : "3: AddSample(input, result)"
Sim -down-> Plot    : "4: PlotDepthProfile(data)"

' Chamadas de segunda ordem
Calc -down-> DB     : "2.1: GetBulkModulus(mineral)"
Plot -down-> ResData : "4.1: GetKGVsDepthFull()"


' --- Notas Explicativas ---
note right of Sim
  <b>Sequência de Interação:</b>
  1. Simulador solicita leitura do arquivo.
  2. Solicita o cálculo da amostra.
     2.1. Calculadora busca dados dos minerais.
  3. Simulador armazena o resultado.
  4. Simulador solicita a geração do gráfico.
     4.1. Gerenciador de plot busca dados.
end note

@enduml
